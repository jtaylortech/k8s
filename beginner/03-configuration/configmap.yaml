apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_NAME: myapp
  APP_ENV: production
  LOG_LEVEL: info
  app.properties: |
    database.url=postgres://db:5432/mydb
    database.pool.size=10
    cache.enabled=true
